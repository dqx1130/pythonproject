m
git init

git add .

git commit -m "注释"

git status：查看当前的状态

关联仓库：
git remote set-url origin  https://<your_token>@github.com/<USERNAME>/<REPO>.git

git push origin main -u (首次）

git push origin main（第二次）

删 addr
git rm -r --cached .